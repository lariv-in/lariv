package p_seer_aisstream

// AISstream messages are ingested from websocket data. Manual create/update
// forms are intentionally omitted for now because typed payloads come from the
// upstream AIS model package and raw rows should stay faithful to stream input.
